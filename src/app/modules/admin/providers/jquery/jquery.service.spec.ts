/*import { TestBed, inject } from '@angular/core/testing';

import { JQ_TOKEN } from './jquery.service';

describe('JQ_TOKEN', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [JQ_TOKEN]
    });
  });

  it('should ...', inject([JQ_TOKEN], (service: JQ_TOKEN) => {
    expect(service).toBeTruthy();
  }));
});
*/